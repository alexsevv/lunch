<h1>Редактирование Первого блюда</h1>
<% if current_user == User.first %>
  <div class="row">
    <div class="col-md-6">
      <%= render 'form', soup: @soup %>
    </div>
  </div>
<% else %>
  <p>Здесь может шариться только Admin</p>
<% end %>
