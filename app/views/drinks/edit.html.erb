<h1>Редактирование Напитка</h1>
<% if current_user == User.first %>
  <div class="row">
    <div class="col-md-6">
      <%= render 'form', drink: @drink %>
    </div>
  </div>
<% else %>
  <p>Здесь может шариться только Admin</p>
<% end %>
